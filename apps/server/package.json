{
  "name": "@coup/server",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/app.js",
  "types": "dist/app.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/app.ts",
    "start": "node dist/app.js",
    "test": "pnpm --filter @coup/game-logic build && vitest run"
  },
  "dependencies": {
    "@coup/game-logic": "workspace:*",
    "@fastify/cors": "^11.0.1",
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
